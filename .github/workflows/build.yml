name: CI — Build & Push

# Déclencheurs :
#   - push sur n'importe quelle branche  → image taguée avec le nom de branche
#   - push d'un tag v*                   → image taguée vX.Y.Z + vX.Y + latest
on:
  push:
    branches:
      - "**"
    tags:
      - "v*"

env:
  REGISTRY: ghcr.io
  # github.repository → "cloud-temple/graph-memory"
  # Image finale    → ghcr.io/cloud-temple/graph-memory:<tag>
  IMAGE_NAME: ${{ github.repository }}

jobs:
  # ─────────────────────────────────────────────────────────────────────────
  # Job — Build multi-arch + push GHCR
  #
  # Stratégie de tags automatique (via docker/metadata-action) :
  #   - branche main       → ghcr.io/cloud-temple/graph-memory:main
  #   - branche dev        → ghcr.io/cloud-temple/graph-memory:dev
  #   - branche feature/xx → ghcr.io/cloud-temple/graph-memory:feature-xx
  #   - tag v2.1.1         → ghcr.io/cloud-temple/graph-memory:2.1.1  :2.1  :latest
  # ─────────────────────────────────────────────────────────────────────────
  build:
    name: Build & Push Docker Image
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write   # Requis pour pousser vers GHCR

    steps:
      - uses: actions/checkout@v4

      # Multi-arch : amd64 natif + arm64 via QEMU (Mac M-series, Pi, etc.)
      - name: Set up QEMU
        uses: docker/setup-qemu-action@v3

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      # Authentification GHCR — GITHUB_TOKEN suffit, aucun secret à configurer
      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      # Génération automatique des tags et labels OCI
      - name: Extract Docker metadata
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}
          tags: |
            # Branches → tag avec le nom de branche (main, dev, feature-xxx, …)
            type=ref,event=branch
            # Tags semver → version complète (ex: v2.1.1 → 2.1.1)
            type=semver,pattern={{version}}
            # Tags semver → major.minor  (ex: v2.1.1 → 2.1)
            type=semver,pattern={{major}}.{{minor}}
            # :latest uniquement sur les tags vX.Y.Z (pas sur les branches)
            type=raw,value=latest,enable=${{ startsWith(github.ref, 'refs/tags/v') }}

      - name: Build and Push
        uses: docker/build-push-action@v6
        with:
          context: .
          platforms: linux/amd64,linux/arm64/v8
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          # Cache BuildKit via GitHub Actions Cache → rebuilds ~3× plus rapides
          cache-from: type=gha
          cache-to: type=gha,mode=max
